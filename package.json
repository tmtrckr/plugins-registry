{
  "name": "time-tracker-plugins-registry",
  "version": "1.0.0",
  "description": "Centralized registry for Time Tracker application plugins",
  "main": "registry.json",
  "scripts": {
    "build": "node scripts/build-registry.js",
    "validate": "npm run build && node scripts/validate.js",
    "validate-plugins": "node scripts/validate-plugins.js",
    "validate-all": "npm run validate-plugins && npm run validate",
    "format": "node scripts/format.js",
    "check-duplicates": "node scripts/check-duplicates.js",
    "create-plugin": "node scripts/create-plugin.js",
    "install-hooks": "node scripts/install-hooks.js"
  },
  "keywords": [
    "time-tracker",
    "plugins",
    "registry",
    "marketplace"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
